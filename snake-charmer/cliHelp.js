'use strict';

const TOOL_OPTIONS =
  '  --shape <name>    Ring shape: circle, stadium, turn, double-turn\n' +
  '                    Default: auto (tries double-turn, falls back to stadium)\n';

module.exports = { TOOL_OPTIONS };
